import { useCallback, useEffect, useState, type FormEvent } from "react";
import { BanknoteArrowDown, CheckCircle2, CircleAlert, Clock3, History, LoaderCircle, RefreshCw, Send, XCircle } from "lucide-react";
import { toast } from "sonner";
import {
  approveMarketplacePayout, completeMarketplacePayout, getAdminMarketplacePayouts,
  getMarketplacePayoutTimeline, rejectMarketplacePayout, startMarketplacePayoutProcessing,
  type MarketplaceAuditTimelineEvent,
} from "../../../api/admin";
import type { CreatorPayout, CreatorPayoutStatus } from "../../../api/marketplace";

const money = new Intl.NumberFormat("vi-VN");
const formatVnd = (value: number) => `${money.format(value)} ₫`;
const TABS: Array<{ value: CreatorPayoutStatus | "ALL"; label: string }> = [
  { value: "ALL", label: "Tất cả" }, { value: "REQUESTED", label: "Chờ duyệt" },
  { value: "APPROVED", label: "Đã duyệt" }, { value: "PROCESSING", label: "Đang chuyển" },
  { value: "COMPLETED", label: "Hoàn tất" }, { value: "REJECTED", label: "Từ chối" }, { value: "FAILED", label: "Lỗi" },
];
const STATUS: Record<CreatorPayoutStatus, { label: string; className: string }> = {
  REQUESTED: { label: "Chờ duyệt", className: "bg-amber-50 text-amber-800 ring-amber-200" },
  APPROVED: { label: "Đã duyệt", className: "bg-sky-50 text-sky-800 ring-sky-200" },
  PROCESSING: { label: "Đang chuyển khoản", className: "bg-violet-50 text-violet-800 ring-violet-200" },
  COMPLETED: { label: "Hoàn tất", className: "bg-emerald-50 text-emerald-800 ring-emerald-200" },
  REJECTED: { label: "Đã từ chối", className: "bg-rose-50 text-rose-800 ring-rose-200" },
  FAILED: { label: "Chuyển khoản lỗi", className: "bg-rose-50 text-rose-800 ring-rose-200" },
};
type Action = "complete" | "reject" | "fail";
const date = (value: string | null) => value ? new Intl.DateTimeFormat("vi-VN", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value)) : "—";
const errorMessage = (error: unknown) => error instanceof Error ? error.message : "Đã có lỗi xảy ra. Vui lòng thử lại.";

function ActionDialog({ action, payout, busy, onClose, onSubmit }: { action: Action; payout: CreatorPayout; busy: boolean; onClose: () => void; onSubmit: (reference: string, paidVndAmount: number, notes: string) => void }) {
  const complete = action === "complete";
  const [reference, setReference] = useState("");
  const [paidVndAmount, setPaidVndAmount] = useState("");
  const [notes, setNotes] = useState("");
  const parsedAmount = Number(paidVndAmount);
  const valid = Boolean(reference.trim()) && (!complete || (Number.isFinite(parsedAmount) && parsedAmount > 0));
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); if (valid) onSubmit(reference.trim(), complete ? parsedAmount : 0, notes.trim()); };
  const title = complete ? "Xác nhận đã chuyển khoản" : action === "reject" ? "Từ chối yêu cầu rút" : "Ghi nhận lỗi chuyển khoản";
  return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4" role="dialog" aria-modal="true"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><h2 className="text-xl font-black text-slate-950">{title}</h2><p className="mt-2 text-sm text-slate-500">{payout.creatorName} · {formatVnd(payout.requestedAmount)}</p><label className="mt-5 block text-sm font-bold text-slate-700">{complete ? "Mã giao dịch ngân hàng" : "Lý do"}<input autoFocus value={reference} onChange={event => setReference(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 font-normal outline-none focus:border-orange-300 focus:ring-4 focus:ring-orange-100" /></label>{complete && <><label className="mt-4 block text-sm font-bold text-slate-700">Số tiền VND đã chuyển<input type="number" min="1" value={paidVndAmount} onChange={event => setPaidVndAmount(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-3 font-normal outline-none focus:border-orange-300 focus:ring-4 focus:ring-orange-100" placeholder="Ví dụ: 80000" /></label><label className="mt-4 block text-sm font-bold text-slate-700">Ghi chú <span className="font-normal text-slate-400">(không bắt buộc)</span><textarea value={notes} onChange={event => setNotes(event.target.value)} className="mt-2 min-h-24 w-full rounded-xl border border-slate-200 p-3 font-normal outline-none focus:border-orange-300 focus:ring-4 focus:ring-orange-100" /></label></>}<div className="mt-6 flex justify-end gap-3"><button type="button" disabled={busy} onClick={onClose} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-700">Hủy</button><button type="submit" disabled={busy || !valid} className={`inline-flex min-w-28 items-center justify-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50 ${complete ? "bg-emerald-600" : "bg-rose-600"}`}>{busy && <LoaderCircle className="h-4 w-4 animate-spin" />}{complete ? "Hoàn tất" : action === "reject" ? "Từ chối" : "Ghi nhận lỗi"}</button></div></form></div>;
}

function TimelineDialog({ events, loading, onClose }: { events: MarketplaceAuditTimelineEvent[]; loading: boolean; onClose: () => void }) {
  return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4" role="dialog" aria-modal="true"><section className="w-full max-w-lg rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><h2 className="text-xl font-black text-slate-950">Nhật ký xử lý</h2><p className="mt-1 text-sm text-slate-500">Lưu lại từng Admin và mốc xử lý payout.</p></div><button type="button" onClick={onClose} className="rounded-xl border border-slate-200 px-3 py-2 text-sm font-bold text-slate-600">Đóng</button></div><div className="mt-6 space-y-4">{loading ? <p className="text-sm text-slate-500">Đang tải nhật ký…</p> : events.length === 0 ? <p className="text-sm text-slate-500">Chưa có hoạt động được ghi nhận.</p> : events.map(event => <div key={event.logId} className="border-l-2 border-orange-200 pl-4"><p className="text-sm font-bold text-slate-800">{event.title ?? event.actionType}</p><p className="mt-1 text-xs text-slate-500">{event.actorName ?? "Hệ thống"} · {date(event.occurredAt)}</p>{event.description && <p className="mt-1 text-sm text-slate-600">{event.description}</p>}</div>)}</div></section></div>;
}

export default function MarketplacePayouts() {
  const [status, setStatus] = useState<CreatorPayoutStatus | "ALL">("REQUESTED");
  const [payouts, setPayouts] = useState<CreatorPayout[]>([]);
  const [loading, setLoading] = useState(true);
  const [failed, setFailed] = useState(false);
  const [busyId, setBusyId] = useState<string | null>(null);
  const [dialog, setDialog] = useState<{ action: Action; payout: CreatorPayout } | null>(null);
  const [timeline, setTimeline] = useState<MarketplaceAuditTimelineEvent[] | null>(null);
  const [timelineLoading, setTimelineLoading] = useState(false);
  const load = useCallback(async () => { setLoading(true); setFailed(false); try { setPayouts(await getAdminMarketplacePayouts(status === "ALL" ? undefined : status)); } catch { setFailed(true); } finally { setLoading(false); } }, [status]);
  useEffect(() => { void load(); }, [load]);
  const transition = async (payout: CreatorPayout, operation: "approve" | "processing") => { setBusyId(payout.payoutId); try { if (operation === "approve") await approveMarketplacePayout(payout.payoutId); else await startMarketplacePayoutProcessing(payout.payoutId); toast.success(operation === "approve" ? "Đã duyệt yêu cầu rút tiền." : "Đã bắt đầu chuyển khoản."); await load(); } catch (error) { toast.error(errorMessage(error)); } finally { setBusyId(null); } };
  const submitDialog = async (reference: string, paidVndAmount: number, notes: string) => { if (!dialog) return; const { action, payout } = dialog; setBusyId(payout.payoutId); try { if (action === "complete") await completeMarketplacePayout(payout.payoutId, reference, paidVndAmount, notes || undefined); else await rejectMarketplacePayout(payout.payoutId, reference, action === "fail"); toast.success("Đã cập nhật yêu cầu rút tiền."); setDialog(null); await load(); } catch (error) { toast.error(errorMessage(error)); } finally { setBusyId(null); } };
  const openTimeline = async (payoutId: string) => { setTimeline([]); setTimelineLoading(true); try { setTimeline(await getMarketplacePayoutTimeline(payoutId)); } catch (error) { toast.error(errorMessage(error)); setTimeline(null); } finally { setTimelineLoading(false); } };
  return <main className="min-h-full bg-[#F7F8FA] p-4 sm:p-7"><div className="mx-auto max-w-7xl"><section className="relative overflow-hidden rounded-[2rem] border border-orange-100 bg-white p-6 shadow-[0_18px_60px_rgba(255,107,0,0.07)] sm:p-8"><div className="pointer-events-none absolute -right-20 -top-24 h-72 w-72 rounded-full bg-orange-100/70 blur-3xl" /><div className="relative flex flex-col gap-5 sm:flex-row sm:items-start sm:justify-between"><div><span className="inline-flex items-center gap-2 rounded-full bg-orange-50 px-3 py-1.5 text-[10px] font-black uppercase tracking-[0.16em] text-[#FF6B00]"><BanknoteArrowDown className="h-3.5 w-3.5" />Marketplace operations</span><h1 className="mt-4 text-3xl font-black tracking-[-0.035em] text-slate-950">Yêu cầu rút tiền</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">Duyệt Creator payout, đối chiếu giao dịch thực tế và xem được lịch sử Admin đã xử lý.</p></div><button type="button" onClick={() => void load()} disabled={loading} className="inline-flex h-10 w-fit items-center gap-2 rounded-xl border border-orange-200 bg-white px-4 text-sm font-bold text-[#FF6B00] hover:bg-orange-50 disabled:opacity-50"><RefreshCw className={`h-4 w-4 ${loading ? "animate-spin" : ""}`} />Làm mới</button></div><div className="relative mt-7 flex gap-2 overflow-x-auto rounded-2xl bg-slate-100 p-1.5">{TABS.map(tab => <button key={tab.value} type="button" onClick={() => setStatus(tab.value)} className={`shrink-0 rounded-xl px-4 py-2.5 text-sm font-bold transition ${status === tab.value ? "bg-white text-[#FF6B00] shadow-sm ring-1 ring-slate-200" : "text-slate-500 hover:bg-white/70"}`}>{tab.label}</button>)}</div></section><section className="mt-6">{loading ? <div className="h-64 animate-pulse rounded-3xl bg-slate-200" /> : failed ? <div className="rounded-3xl border border-rose-100 bg-white p-10 text-center"><CircleAlert className="mx-auto h-9 w-9 text-rose-500" /><h2 className="mt-4 text-xl font-black">Không thể tải yêu cầu rút tiền</h2><button type="button" onClick={() => void load()} className="mt-5 rounded-xl bg-[#FF6B00] px-4 py-2.5 text-sm font-bold text-white">Thử lại</button></div> : payouts.length === 0 ? <div className="rounded-3xl border border-dashed border-slate-200 bg-white px-6 py-16 text-center"><Clock3 className="mx-auto h-10 w-10 text-slate-300" /><h2 className="mt-4 text-xl font-black text-slate-900">Chưa có yêu cầu phù hợp</h2></div> : <div className="grid gap-4">{payouts.map(payout => { const meta = STATUS[payout.status]; const busy = busyId === payout.payoutId; return <article key={payout.payoutId} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-col gap-5 lg:flex-row lg:items-center lg:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-black text-slate-950">{payout.creatorName}</h2><span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ring-1 ${meta.className}`}>{meta.label}</span></div><p className="mt-1 truncate text-sm text-slate-500">{payout.creatorEmail}</p><div className="mt-4 grid gap-3 text-sm sm:grid-cols-3"><div><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Yêu cầu</p><p className="mt-1 text-lg font-black text-[#D94E00]">{formatVnd(payout.requestedAmount)}</p></div><div><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Đã chuyển</p><p className="mt-1 font-bold text-slate-800">{payout.paidVndAmount ? formatVnd(payout.paidVndAmount) : "—"}</p></div><div><p className="text-xs font-bold uppercase tracking-wide text-slate-400">Tạo lúc</p><p className="mt-1 font-bold text-slate-800">{date(payout.createdAt)}</p></div></div>{payout.externalTransferReference && <p className="mt-4 text-sm text-slate-600">Mã giao dịch: <b>{payout.externalTransferReference}</b></p>}</div><div className="flex shrink-0 flex-wrap gap-2 lg:max-w-64 lg:justify-end"><button type="button" onClick={() => void openTimeline(payout.payoutId)} className="inline-flex h-10 items-center gap-2 rounded-xl border border-slate-200 px-3 text-xs font-bold text-slate-700 hover:border-orange-200 hover:text-[#FF6B00]"><History className="h-3.5 w-3.5" />Nhật ký</button>{payout.status === "REQUESTED" && <><button type="button" disabled={busy} onClick={() => void transition(payout, "approve")} className="inline-flex h-10 items-center rounded-xl bg-emerald-600 px-3 text-xs font-bold text-white disabled:opacity-50">Duyệt</button><button type="button" disabled={busy} onClick={() => setDialog({ action: "reject", payout })} className="inline-flex h-10 items-center rounded-xl border border-rose-200 px-3 text-xs font-bold text-rose-700">Từ chối</button></>}{payout.status === "APPROVED" && <button type="button" disabled={busy} onClick={() => void transition(payout, "processing")} className="inline-flex h-10 items-center rounded-xl bg-[#FF6B00] px-3 text-xs font-bold text-white">Bắt đầu chuyển</button>}{payout.status === "PROCESSING" && <><button type="button" disabled={busy} onClick={() => setDialog({ action: "complete", payout })} className="inline-flex h-10 items-center rounded-xl bg-emerald-600 px-3 text-xs font-bold text-white">Hoàn tất</button><button type="button" disabled={busy} onClick={() => setDialog({ action: "fail", payout })} className="inline-flex h-10 items-center rounded-xl border border-rose-200 px-3 text-xs font-bold text-rose-700">Báo lỗi</button></>}</div></div></article>; })}</div>}</section></div>{dialog && <ActionDialog action={dialog.action} payout={dialog.payout} busy={busyId === dialog.payout.payoutId} onClose={() => setDialog(null)} onSubmit={submitDialog} />}{timeline !== null && <TimelineDialog events={timeline} loading={timelineLoading} onClose={() => setTimeline(null)} />}</main>;
}
