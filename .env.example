# Backend API URL
# Uncomment and set this for production
# VITE_API_URL=http://localhost:8080

# For development, the app will use client-side date (new Date())
# To use backend date, set VITE_API_URL and uncomment the fetch call in src/api/dateService.ts
